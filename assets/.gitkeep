# This file exists only so Git tracks the otherwise-empty `assets` folder.
# Git does not track empty directories on its own.
#
# Put future images, icons, or other static files (like a logo) in here.
# You can delete this file once real assets are added.
